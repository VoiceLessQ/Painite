org.gradle.jvmargs=-Xmx1G
org.gradle.parallel=true
org.gradle.configuration-cache=false

# MC 26.x needs JDK 25. Point Gradle at it from ~/.gradle/gradle.properties
# (org.gradle.java.home=/path/to/jdk-25), never from this file.

# 26.x ships pre-deobfed via the NeoForge maven; tell Loom not to remap.
fabric.loom.disableObfuscation=true

# Mappings: official Mojmap. Versions checked against Ferrite 2026-09-06.
minecraft_version=26.3-pre-1
loader_version=0.19.5
loom_version=1.14-SNAPSHOT

mod_version=0.1.0-alpha+26.3
maven_group=me.apika
archives_base_name=painite

fabric_version=0.159.2+26.3
