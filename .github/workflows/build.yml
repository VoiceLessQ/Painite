# Cross-platform native build + jar assembly.
#
# Four jobs:
#   build-native-windows — mingw-w64 GCC via setup-msys2, cargo → painite.dll
#   build-native-linux   — system GCC,                    cargo → libpainite.so
#   build-native-macos   — system Clang,                  cargo → libpainite.dylib
#   assemble-jar         — downloads all three natives, runs ./gradlew build
#                          with native tasks skipped (pre-placed artifacts)
#
# The final jar ships with all three binaries under
# assets/painite/natives/{windows,linux,macos}/. PainiteNative.java picks
# the one matching the host OS at runtime and falls back cleanly on
# unsupported platforms.

name: build
on: [pull_request, push]

jobs:
  build-native-windows:
    runs-on: windows-latest
    steps:
      - name: checkout
        uses: actions/checkout@v4
      - name: setup msys2 + mingw-w64 gcc
        uses: msys2/setup-msys2@v2
        with:
          msystem: MINGW64
          install: mingw-w64-x86_64-gcc
          path-type: inherit
      - name: cargo build (x86_64-pc-windows-gnu)
        shell: bash
        env:
          CARGO_TARGET_X86_64_PC_WINDOWS_GNU_LINKER: gcc
          CARGO_TARGET_X86_64_PC_WINDOWS_GNU_AR: ar
        run: cargo build --release --target x86_64-pc-windows-gnu --manifest-path=rust/mod/Cargo.toml
      - name: upload native
        uses: actions/upload-artifact@v4
        with:
          name: native-windows
          path: target/x86_64-pc-windows-gnu/release/painite.dll
          if-no-files-found: error

  build-native-linux:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v4
      - name: cargo clippy (deny-level lints gate the build, tests included)
        shell: bash
        run: |
          rustup component add clippy
          cargo clippy --workspace --all-targets --release --target x86_64-unknown-linux-gnu
      - name: cargo test
        shell: bash
        run: cargo test --workspace --release --target x86_64-unknown-linux-gnu
      - name: cargo build (x86_64-unknown-linux-gnu)
        shell: bash
        run: cargo build --release --target x86_64-unknown-linux-gnu --manifest-path=rust/mod/Cargo.toml
      - name: upload native
        uses: actions/upload-artifact@v4
        with:
          name: native-linux
          path: target/x86_64-unknown-linux-gnu/release/libpainite.so
          if-no-files-found: error

  build-native-linux-aarch64:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v4
      - name: install gcc-aarch64-linux-gnu
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc-aarch64-linux-gnu
      - name: add aarch64 rust target
        run: rustup target add aarch64-unknown-linux-gnu
      - name: cargo build (aarch64-unknown-linux-gnu)
        shell: bash
        env:
          CARGO_TARGET_AARCH64_UNKNOWN_LINUX_GNU_LINKER: aarch64-linux-gnu-gcc
        run: cargo build --release --target aarch64-unknown-linux-gnu --manifest-path=rust/mod/Cargo.toml
      - name: upload native
        uses: actions/upload-artifact@v4
        with:
          name: native-linux-aarch64
          path: target/aarch64-unknown-linux-gnu/release/libpainite.so
          if-no-files-found: error

  build-native-macos:
    runs-on: macos-latest
    steps:
      - name: checkout
        uses: actions/checkout@v4
      - name: add x86_64 rust target
        run: rustup target add x86_64-apple-darwin
      - name: cargo build (aarch64-apple-darwin)
        shell: bash
        run: cargo build --release --target aarch64-apple-darwin --manifest-path=rust/mod/Cargo.toml
      - name: cargo build (x86_64-apple-darwin)
        shell: bash
        run: cargo build --release --target x86_64-apple-darwin --manifest-path=rust/mod/Cargo.toml
      - name: lipo into universal binary
        shell: bash
        run: |
          lipo -create \
            target/aarch64-apple-darwin/release/libpainite.dylib \
            target/x86_64-apple-darwin/release/libpainite.dylib \
            -output target/libpainite.dylib
          # sanity-check both slices are present
          lipo -info target/libpainite.dylib
      - name: upload native
        uses: actions/upload-artifact@v4
        with:
          name: native-macos
          path: target/libpainite.dylib
          if-no-files-found: error

  assemble-jar:
    needs: [build-native-windows, build-native-linux, build-native-linux-aarch64, build-native-macos]
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v4
      - name: validate gradle wrapper
        uses: gradle/actions/wrapper-validation@v4
      - name: setup jdk 25
        uses: actions/setup-java@v4
        with:
          java-version: 25
          distribution: 'temurin'
      - name: download windows native
        uses: actions/download-artifact@v4
        with:
          name: native-windows
          path: src/main/resources/assets/painite/natives/windows
      - name: download linux native
        uses: actions/download-artifact@v4
        with:
          name: native-linux
          path: src/main/resources/assets/painite/natives/linux
      - name: download linux aarch64 native
        uses: actions/download-artifact@v4
        with:
          name: native-linux-aarch64
          path: src/main/resources/assets/painite/natives/linux-aarch64
      - name: download macos native
        uses: actions/download-artifact@v4
        with:
          name: native-macos
          path: src/main/resources/assets/painite/natives/macos
      - name: assemble jar (skip rust build — natives already placed)
        shell: bash
        run: ./gradlew build -x buildRustLib -x copyRustDll
      - name: upload jar
        uses: actions/upload-artifact@v4
        with:
          name: Artifacts
          path: build/libs/

  # Auto-publishes a GitHub Release when a tag matching v* is pushed.
  # Body is taken from README.md at the tag commit
  # (always describes what shipped at that version). Pre-release flag is
  # auto-set if the tag name contains "alpha" or "beta".
  #
  # Bypass: don't push a tag and this job never runs. Manual releases
  # via the web UI still work the same way.
  release:
    needs: assemble-jar
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: checkout (need README.md at tag commit)
        uses: actions/checkout@v4
      - name: download jar artifact
        uses: actions/download-artifact@v4
        with:
          name: Artifacts
          path: jars/
      - name: publish release
        uses: softprops/action-gh-release@v2
        with:
          name: "Painite ${{ github.ref_name }}"
          body_path: README.md
          files: jars/painite-*.jar
          prerelease: ${{ contains(github.ref_name, 'alpha') || contains(github.ref_name, 'beta') }}
          fail_on_unmatched_files: true
